// import { API_CONFIG, API_ENDPOINTS } from "@/config/api";
// import type { Comments } from "@/types/project";
// import { useCallback } from "react";

export const useCommentsApi = () => {
  // TO BE IMPLEMENTED
  // const fetchCommentsForProject = useCallback(
  //   async (projectId: string): Promise<Comments[]> => {
  //     const response = await fetch(
  //       `${API_CONFIG.baseURL}${API_ENDPOINTS.project}/${projectId}/comments`,
  //       {
  //         method: "GET",
  //         headers: API_CONFIG.headers,
  //       }
  //     );
  //     if (!response.ok) {
  //       throw new Error(
  //         `Erro ao buscar os comentários: ${response.statusText}`
  //       );
  //     }
  //     const data: Comments[] = await response.json();
  //     return data;
  //   },
  //   []
  // );
  // return { fetchCommentsForProject };
};
